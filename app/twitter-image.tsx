// X reads twitter:image in preference to og:image, so the same card is
// published under both names rather than relying on its og fallback.
export { default, size, contentType, alt } from "./opengraph-image";
